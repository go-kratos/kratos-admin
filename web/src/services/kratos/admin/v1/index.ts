// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */

// LoginRequest is the request message for the Login method.
export type LoginRequest = {
  // Username of the user.
  username: string | undefined;
  // Password of the user.
  password: string | undefined;
};

// LoginReply is the reply message for the Login method.
export type LoginReply = {
  // Username of the logged-in user.
  username: string | undefined;
};

// CurrentRequest is the request message for the Current method.
export type CurrentRequest = {
};

// CurrentReply is the reply message for the Current method.
export type CurrentReply = {
  // Username of the currently logged-in user.
  username: string | undefined;
};

export type LogoutRequest = {
};

export type LogoutReply = {
};

// The Auth service definition.
export interface Auth {
  // Login a user and return the username.
  Login(request: LoginRequest): Promise<LoginReply>;
  // Current returns the currently logged-in user.
  Current(request: CurrentRequest): Promise<CurrentReply>;
  // Logout the currently logged-in user.
  Logout(request: LogoutRequest): Promise<LogoutReply>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType) => Promise<unknown>;

export function createAuthClient(
  handler: RequestHandler
): Auth {
  return {
    Login(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/auths/login`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }) as Promise<LoginReply>;
    },
    Current(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/auths/current`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }) as Promise<CurrentReply>;
    },
    Logout(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/auths/logout`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }) as Promise<LogoutReply>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
